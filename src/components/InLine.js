import React from 'react'

// Inline CSS

const heading = {
    fontSize: '80px',
    color: 'blue'
}

function InLine() {
    return (
        <div>
            <h2 style = {heading}>Inline</h2>
        </div>
    )
}

export default InLine